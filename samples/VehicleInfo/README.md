# VehicleInfo — Service 0x09 vehicle information

Demonstrates the `TOBDVIN` component which fronts Service 0x09
(VIN, Calibration IDs, CVNs, In-Use Performance Tracking, ECU
Name, Engine Serial Number) and its UDS equivalent (DID 0xF190).

```sh
dcc32 -B VehicleInfo.dpr
VehicleInfo
```
