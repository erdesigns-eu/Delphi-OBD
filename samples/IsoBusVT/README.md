# IsoBusVT — ISO 11783-6 Virtual Terminal

Walk-through of the `TOBDIsoBusVT` surface. See
`src/Speciality/OBD.Speciality.IsoBus.VT.pas` for the
object-pool / soft-key / input-attribute API.

```sh
dcc32 -B IsoBusVT.dpr
IsoBusVT
```
