# LINSchedule — LIN schedule-table walk-through

Builds a synthetic `TOBDLDFSchedule` in memory and prints the
slot timing. For full LDF parsing see sample `04-LIN-LDF-Parse`.

```sh
dcc32 -B LINSchedule.dpr
LINSchedule
```
